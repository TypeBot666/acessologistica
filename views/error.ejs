<!DOCTYPE html>
<html lang="pt-BR">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Erro - WhatsApp Sender</title>
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css">
</head>
<body>
  <div class="container mt-5">
    <div class="row justify-content-center">
      <div class="col-md-8">
        <div class="card">
          <div class="card-header bg-danger text-white">
            <h4 class="mb-0">Erro</h4>
          </div>
          <div class="card-body">
            <h5 class="card-title">Ocorreu um erro</h5>
            <p class="card-text"><%= error.message %></p>
            <% if (error.stack && process.env.NODE_ENV === 'development') { %>
              <div class="mt-4">
                <h6>Stack Trace:</h6>
                <pre class="bg-light p-3 rounded"><%= error.stack %></pre>
              </div>
            <% } %>
            <a href="/admin" class="btn btn-primary mt-3">Voltar para o Dashboard</a>
          </div>
        </div>
      </div>
    </div>
  </div>
</body>
</html>
